name: "广告拦截"
desc: "去广告规则"

rule-providers:
  AntiAD:
    type: http
    behavior: domain
    url: "https://raw.githubusercontent.com/Asimpleuncle/simple/main/AdRule/AdRuleDomain.yaml"
    path: ./ruleset/AdRuleDomain.yaml
    interval: 86400

rules:
  - RULE-SET,AntiAD,REJECT
